name: thalo
description: Thalo - multilingual social media app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cupertino_icons: ^1.0.6
  intl_phone_number_input: ^0.7.4
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
